# CH5 공부노트

> 💡HTTP와 WebSocket 차이점
> 
1. 편지와 전화통화의 차이
- HTTP (지난주): "편지"와 같습니다. 클라이언트가 서버에 요청(Request)을 보내야만, 서버가 응답(Response)을 줍니다. 용건이 끝나면 연결을 바로 끊어버리죠. 실시간으로 계속 데이터를 주고받기에는 매번 연결을 맺고 끊어야 해서 네트워크 낭비가 심하고 느립니다
- WebSocket (이번주): "전화통화"와 같습니다. 처음에 한 번 전화를 걸어 연결을 맺어두면, 끊을 때까지 파이프라인이 계속 열려 있습니다. 클라이언트가 묻지 않아도 서버가 먼저 데이터를 보낼 수 있고, 양방향으로 지연 없이(실시간) 데이터를 주고받을 수 있습니다. 실시간 멀티플레이어나, AI 모델이 실시간으로 분석한 좌표를 언리얼로 스트리밍할 때 필수적
2. 핸드셰이크(Handshake)란?
- 처음 연결을 시도할 때는 웹소켓도 HTTP 방식을 빌려 씁니다. "우리 이제부터 HTTP 말고 WebSocket으로 실시간 통신하자!"라고 합의를 보는 과정인데, 이를 핸드셰이크라고 부릅니다. 이 과정이 성공적으로 끝나면 본격적으로 실시간 데이터 파이프가 열리게 됩니다
---

> 💡WebSocket의 구조와 동작 원리
> 
1. WebSocket의 생명주기
- 1단계: 핸드셰이크 (Handshake - HTTP로 첫인사)
    - 클라이언트(언리얼 엔진)가 서버에게 HTTP 형태로 "우리 이제 웹소켓으로 통신할래?" 라고 제안합니다.
    - 서버가 수락하면 상태 코드 101 (Switching Protocols)을 반환하며 HTTP 연결이 웹소켓 연결로 업그레이드됩니다.
- 2단계: 연결 유지 (Open - 파이프라인 개통)
    - 성공적으로 연결되면 양쪽을 잇는 전용 도로(터널)가 뚫립니다.
    - 이때부터는 연결을 명시적으로 끊기 전까지 터널이 계속 유지됩니다.
- 3단계: 양방향 데이터 전송 (Message - 데이터 핑퐁)
    - 가장 중요한 단계입니다. 클라이언트가 요청하지 않아도, 서버가 원할 때 언제든 데이터를 쏠 수 있습니다.
    - 데이터는 보통 텍스트(주로 JSON)나 바이너리(0과 1의 이진 데이터) 형태로 가볍게 전송됩니다. HTTP처럼 무거운 헤더(Header)가 매번 붙지 않아 압도적으로 빠릅니다.
- 4단계: 연결 종료 (Close - 파이프 닫기)
    - 클라이언트나 서버 중 한 곳에서 "이제 그만 통신하자"고 신호를 보내면 터널이 닫힙니다.
    - 게임이 종료되거나, 레벨을 이동할 때 직접 닫아주는 로직이 필요합니다.
---

> 💡HTTP vs WebSocket (왜 게임/AI 연동에 필수인가?)
> 
| 구분 | HTTP (Polling) | WebSocket |
|---|---|---|
| 작동 방식 | 클라이언트가 0.1초마다 "데이터 있어?" 계속 물어봄 | 파이프를 열어두고 서버가 "데이터 왔어!" 하고 그냥 줌 |
| 서버 부하 | 질문마다 연결/해제를 반복하여 트래픽 낭비 극심 | 최초 1회 연결만 하므로 가벼움 |
| 통신 방향 | 단방향<br>(클라이언트 → 서버 요청 필수) | 양방향<br>(서버 → 클라이언트 먼저 전송 가능) |
| 활용 예시 | 날씨 API, 환율 정보, 일회성 DB 저장<br>(W4 내용) | 실시간 AI 위치 트래킹, 멀티플레이어 동기화, 채팅<br>(이번 주 목표) |
---

> 💡웹소켓 객체 생성 -> 이벤트 바인딩 -> 접속 -> 메시지 발송 로직
> 
| 핵심 키워드 | 언리얼 C++에서의 역할 | 동작 비유 |
|---|---|---|
| `WebSocketsModule.h` | 웹소켓 기능을 사용하기 위해 꼭 포함해야 하는 헤더 파일 | 통신사 기지국 |
| `IWebSocket` | 웹소켓 통신 파이프 그 자체를 관리하는 인터페이스 | 개통된 스마트폰 |
| `CreateWebSocket()` | 접속할 서버 주소(`ws://...`)를 넣고 웹소켓 객체를 생성 | 전화기 구매 및 번호 입력 |
| `OnConnected()` | 연결이 성공했을 때 실행될 이벤트(Delegate) | 통화 연결 후 "여보세요?" |
| `Connect()` | 설정한 서버로 실제 접속 시도를 시작하는 함수 | 통화 버튼 누르기 |
| `Send()` | 서버로 문자열(JSON 등)을 보내는 함수 | 말하기 |
---

> 💡웹소켓 객체 송수신 로직
> 
1. OnMessage (귀 기울이기)
- 역할: 서버에서 언리얼로 데이터를 보냈을 때 발생하는 이벤트입니다.
- 특징: 서버가 언제 데이터를 보낼지 모르기 때문에, 이 이벤트가 호출될 때 문자열(FString) 데이터를 받아 처리하는 콜백 함수를 등록해 두어야 합니다.
2. OnClosed (이별 감지하기)
- 역할: 서버가 꺼지거나, 네트워크가 끊겨서 웹소켓 연결이 완전히 닫혔을 때 발생합니다.
- 매개변수: 상태 코드(StatusCode), 이유(Reason), 정상 종료 여부(bWasClean)를 반환하므로 왜 끊겼는지 로그로 추적할 수 있습니다.
3. 재연결 로직 (심폐소생술)과 타이머의 필요성
- 인터넷 연결은 언제든 불안정해질 수 있습니다. 끊기자마자 while문 같은 반복문으로 즉시 재연결(Connect())을 시도하면 게임 스레드가 얼어버리거나(Freeze) 엔진이 뻗어버립니다(Crash).
- 따라서 "3초 뒤에 다시 전화 걸어봐"라고 엔진의 타이머 매니저(FTimerManager)에게 부탁하는 비동기 방식으로 구현해야 안전합니다.
---

> 💡JSON 데이터를 웹소켓으로 주고받기(직렬화/역직렬화)
>
- C++의 구조체나 객체는 메모리에 아주 복잡하게 얽혀 있어서 통신선(웹소켓)을 통해 그대로 보낼 수 없습니다. 그래서 데이터를 일렬로 길게 늘어뜨린 문자열(String) 형태로 변환해야 하는데, 이 규격 중 전 세계에서 가장 많이 쓰이는 것이 바로 JSON입니다.
1. FJsonObject (JSON 객체 본체):
- 데이터를 Key : Value 형태로 담는 바구니입니다.
- SetStringField(), GetNumberField() 등의 함수로 데이터를 넣고 뺍니다.
2. TJsonWriter / TJsonReader (번역가):
- Writer: FJsonObject 바구니에 담긴 데이터를 하나의 긴 텍스트(FString)로 써 내려갑니다. (직렬화용)
- Reader: 서버에서 날아온 긴 텍스트를 읽어서 분석할 준비를 합니다. (역직렬화용)
3. FJsonSerializer (공장장):
- 번역가(Writer/Reader)를 데리고 실제 변환 작업을 실행하는 핵심 관리자입니다.
---

> 💡HTTP와 Websocket 콤보
>
1. WebSocket만 쓰면 안 되나요?
- 웹소켓은 "변화된 데이터(Delta)"를 빠르게 쏘는 데 특화되어 있습니다. 게임에 처음 접속했을 때 수백 명의 캐릭터 위치, 맵 상태 등 거대한 '초기 데이터'를 웹소켓으로 받으려면 실시간 통신 파이프가 꽉 막혀버릴 수 있습니다.
2. 환상의 콤보:
- HTTP (REST API): 무겁고 정적인 초기 세팅 데이터를 한 번에 확 다운로드 받습니다. (예: "현재 게임에 접속해 있는 유저 목록과 초기 좌표 다 가져와!")
- WebSocket: 초기 세팅이 끝나면, 그때부터 발생하는 가벼운 실시간 움직임만 주고받습니다. (예: "A유저가 1m 앞으로 이동함")
---

> 💡실행 흐름 설계가 생명
>
- BeginPlay 발생: 가장 먼저 HTTP GET 요청을 서버로 보냅니다. (이때 웹소켓은 아직 건드리지 않습니다.)
- HTTP 응답 대기: 서버에서 초기 데이터를 줄 때까지 기다립니다.
- 초기 세팅 완료: HTTP 응답(OnResponseReceived)이 오면, JSON을 파싱해서 화면에 캐릭터들을 스폰시킵니다.
- 웹소켓 연결 시작: 초기 맵 세팅이 완벽히 끝난 직후에, InitializeWebSocket()을 호출하여 실시간 통신 파이프를 엽니다.